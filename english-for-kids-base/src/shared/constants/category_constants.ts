import { categoriesData } from '../input_data/cards_data/cards-data';

const CATEGORY_CONSTANTS = {
  categories: Object.values(categoriesData), // массив объектов категорий со свойствами категорий
};

export default CATEGORY_CONSTANTS;
